import { SurfaceAsegura } from "@/components/surface-asegura";

/** Web-chat de Colsubsidio (canal rico). Público, sin login. */
export const dynamic = "force-dynamic";

export default function WebChatPage() {
  return <SurfaceAsegura />;
}
